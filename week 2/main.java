import java.io.IOException;




public class main {

	public static void main(String[] args) throws IOException, ClassCastException, ClassNotFoundException {
		/* Parser ( Week 1 ) currently disabled.
		 * 
			Parser p = new Parser();
			if(p.parseDir(root) != 0)
				System.out.println("Error: parseDir");
		 */

		new Annotator();
	}
}
